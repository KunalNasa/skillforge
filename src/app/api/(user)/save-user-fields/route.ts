export async function POST(request: Request) {
    try {
        const { other_details,
            progress: number
            date_created: Date
        goal: string
        current_status: string
        profile_picture: string
        } catch (error) {

        }
    }